<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/profile.css">
    <title>LatMia – Meu perfil</title>
</head>

<body>
    <header class="site-header">
        <span class="header-logo">
            <img src="/assets/images/logo_dog_and_cat.webp" alt="LatMia">
        </span>
        <nav class="header-nav">
            <a class="switch_page" href="/gatos">🐱 Gatos</a>
            <a class="switch_page" href="/caes">🐶 Cães</a>
            <a href="/meus_anuncios">Meus anúncios</a>
            <a href="/anunciar" class="nav-cta">+ Anunciar</a>
            <a href="/logout" style="color: #DC2626; font-weight: 600; margin-left: 1rem;">Sair</a>
        </nav>
    </header>

    <main>
        <div class="profile-layout">

            <aside class="profile-card">
                <div class="avatar-wrap">
                    <div class="avatar"><%= user.nome.substring(0, 2).toUpperCase() %></div>
                </div>
                <h2 class="profile-name"><%= user.nome %></h2>
                <p class="profile-email"><%= user.email %></p>
                <p class="profile-since">Membro LatMia · <%= user.estado || 'DF' %></p>

                <div class="profile-stats">
                    <div class="profile-stat">
                        <span class="profile-stat-number"><%= totalAnuncios %></span>
                        <span class="profile-stat-label">Anúncios ativos</span>
                    </div>
                    <div class="profile-stat">
                        <span class="profile-stat-number"><%= pedidos.length %></span>
                        <span class="profile-stat-label">Pedidos enviados</span>
                    </div>
                </div>

                <a href="/meus_anuncios" class="profile-link">Ver meus anúncios →</a>
            </aside>

            <div class="profile-content">

                <div class="tab-bar">
                    <button class="tab active" data-tab="dados">Dados pessoais</button>
                    <button class="tab" data-tab="seguranca">Segurança</button>
                    <button class="tab" data-tab="pedidos">Meus pedidos de adoção</button>
                </div>

                <div class="tab-panel active" id="tab-dados">
                    <section class="form-section">
                        <h2 class="section-title">Informações pessoais</h2>
                        <p class="section-hint">Esses dados aparecem para outros usuários quando você anuncia um animal.</p>

                        <div class="field-row">
                            <div class="field-group">
                                <label class="field-label" for="nome">Nome completo</label>
                                <input type="text" id="nome" name="nome" class="field-input" value="<%= user.nome %>">
                            </div>
                        </div>

                        <div class="field-row">
                            <div class="field-group">
                                <label class="field-label" for="email">Email</label>
                                <input type="email" id="email" class="field-input" value="<%= user.email %>" readonly style="background: #f3f4f6; cursor: not-allowed;" title="O email não pode ser alterado">
                            </div>
                            <div class="field-group">
                                <label class="field-label" for="telefone">Telefone</label>
                                <input type="tel" id="telefone" name="telefone" class="field-input" value="<%= user.telefone || '' %>">
                            </div>
                        </div>

                        <div class="field-row">
                            <div class="field-group field-group--narrow">
                                <label class="field-label" for="estado">Estado</label>
                                <select id="estado" name="estado" class="field-input">
                                    <% const estados = ['AC','AL','AP','AM','BA','CE','DF','ES','GO','MA','MT','MS','MG','PA','PB','PR','PE','PI','RJ','RN','RS','RO','RR','SC','SP','SE','TO']; %>
                                    <% estados.forEach(est => { %>
                                        <option value="<%= est %>" <%= user.estado === est ? 'selected' : '' %>><%= est %></option>
                                    <% }) %>
                                </select>
                            </div>
                        </div>

                        <div class="form-actions">
                            <button type="button" class="btn-primary">Salvar alterações</button>
                        </div>
                    </section>
                </div>

                <div class="tab-panel" id="tab-seguranca">
                    <section class="form-section">
                        <h2 class="section-title">Alterar senha</h2>
                        <div class="field-row">
                            <div class="field-group">
                                <label class="field-label" for="senha-atual">Senha atual</label>
                                <input type="password" id="senha-atual" class="field-input" placeholder="••••••••">
                            </div>
                        </div>
                        <div class="field-row">
                            <div class="field-group">
                                <label class="field-label" for="senha-nova">Nova senha</label>
                                <input type="password" id="senha-nova" class="field-input" placeholder="••••••••">
                            </div>
                            <div class="field-group">
                                <label class="field-label" for="senha-confirma">Confirmar nova senha</label>
                                <input type="password" id="senha-confirma" class="field-input" placeholder="••••••••">
                            </div>
                        </div>
                        <div class="form-actions">
                            <button type="button" class="btn-primary">Atualizar senha</button>
                        </div>
                    </section>
                </div>

                <div class="tab-panel" id="tab-pedidos">
                    <section class="form-section">
                        <h2 class="section-title">Pedidos de adoção enviados</h2>

                        <div class="pedido-list">
                            <% if (pedidos && pedidos.length > 0) { %>
                                <% pedidos.forEach(pedido => { %>
                                    <div class="pedido-row">
                                        <img class="pedido-photo" src="<%= pedido.url_foto || (pedido.especie === 'cao' ? '/assets/images/logo_dog.webp' : '/assets/images/logo_cat.webp') %>" alt="<%= pedido.animal_nome %>">
                                        <div class="pedido-info">
                                            <h3><%= pedido.animal_nome %></h3>
                                            <p class="pedido-meta"><%= pedido.especie === 'cao' ? 'Cão' : 'Gato' %> · <%= pedido.animal_estado || 'DF' %></p>
                                            <p class="pedido-date">
                                                Enviado em <%= new Date(pedido.data_criacao).toLocaleDateString('pt-BR', {day: 'numeric', month: 'short', year: 'numeric'}) %>
                                            </p>
                                        </div>
                                        
                                        <% let badgeClass = 'status-pendente'; let badgeText = 'Pendente'; %>
                                        <% if (pedido.status_adocao === 'aprovado') { badgeClass = 'status-aprovado'; badgeText = 'Aprovado'; } %>
                                        <% if (pedido.status_adocao === 'recusado') { badgeClass = 'status-recusado'; badgeText = 'Recusado'; } %>
                                        <% if (pedido.status_adocao === 'em_analise') { badgeClass = 'status-em_analise'; badgeText = 'Em análise'; } %>
                                        
                                        <span class="status-badge <%= badgeClass %>"><%= badgeText %></span>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <div style="text-align: center; padding: 2rem; color: #6B7280;">
                                    <p>Você ainda não enviou nenhum pedido de adoção. 🐾</p>
                                    <a href="/caes" style="color: var(--color-blue); font-weight: 600; text-decoration: none;">Ver cães disponíveis</a>
                                </div>
                            <% } %>
                        </div>
                    </section>
                </div>

            </div>
        </div>
    </main>

    <script>
        document.querySelectorAll('.tab').forEach(tab => {
            tab.addEventListener('click', () => {
                document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
                document.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
                tab.classList.add('active');
                document.getElementById('tab-' + tab.dataset.tab).classList.add('active');
            });
        });
    </script>
</body>

</html>